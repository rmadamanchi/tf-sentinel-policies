import "tfconfig"

restrict_resources = func() {
  validated = true

  if length(tfconfig.resources) > 0 {
    print("Resources not allowed", tfconfig.resources)
    validated = false
  }

  return validated
}

resources_restricted = restrict_resources()

main = rule {
  resources_restricted
}
